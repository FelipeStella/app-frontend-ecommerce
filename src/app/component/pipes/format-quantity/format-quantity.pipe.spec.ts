import { FormatQuantityPipe } from './format-quantity.pipe';

describe('FormatQuantityPipe', () => {
  it('create an instance', () => {
    const pipe = new FormatQuantityPipe();
    expect(pipe).toBeTruthy();
  });
});
